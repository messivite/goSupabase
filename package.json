{
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:install": "npm ci",
    "docs:all": "npm ci && npm run docs:build && npm run docs:preview",
    "docs:verify": "npm ci && npm run docs:build"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  }
}
