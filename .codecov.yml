# https://docs.codecov.com/docs/codecovyml-reference
coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        # Per-commit "lines in diff covered" is noisy for small or CLI-heavy
        # changes; keep patch visible in Codecov UI without failing CI.
        informational: true
